{
	"name": "repo_api",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"getRepo": "gh repo list emily-ly-san --json name,description,url --jq \"to_entries | map({id: (.key + 1)} + .value)\" > ./data.json"
	},

	"author": "",
	"license": "ISC",
	"description": ""
}
